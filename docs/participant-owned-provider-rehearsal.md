# Provider rehearsal evidence

An earlier provider rehearsal stopped before provider mutation. It predates the
current workflow and is retained only as historical context.

The live participant-owned `vercel-test-3` rehearsal completed: the installer
created a participant repository on `main`; GitHub-triggered Vercel production
reached READY; Neon migrations applied; canonical HTTPS worked; and browser
sign-up, sign-out, sign-in, session persistence, and authenticated dashboard
access passed. No credentials or connection URLs are retained in this evidence.
